/**
 * Hospital Portal layout — facility management pages.
 * Wraps: dashboard, profile editor, availability updates.
 * Authentication will be enforced in a later phase.
 */
export default function PortalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
